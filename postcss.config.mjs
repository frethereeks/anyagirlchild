/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

export default config;

// Tailwind v4 Config
// const config = {
//   plugins: ["@tailwindcss/postcss"],
// };

// export default config;